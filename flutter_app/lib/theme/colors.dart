import 'package:flutter/material.dart';

/// Brand and semantic colors — soft wellness palette (not saturated “acid” UI).
class AppColors {
  /// Primary seed: calm periwinkle / digital wellbeing.
  static const Color seed = Color(0xFF6B6FD9);

  /// App canvas — airy lavender-grey.
  static const Color background = Color(0xFFF4F2FA);

  static const Color primary = seed;
  static const Color error = Color(0xFFC45C5C);
  static const Color accent = Color(0xFF5A8F7B);
  static const Color card = Color(0xFFFFFFFF);
  static const Color textPrimary = Color(0xFF2A2638);
  static const Color textSecondary = Color(0xFF6B6578);
  static const Color shadow = Color(0x226B6FD9);
}
